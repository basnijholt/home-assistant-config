purge_keep_days: 5
exclude:
  entities:
    - sun.sun  # Don't record sun data
    - sensor.desk_lamp_current
    - sensor.desk_lamp_watts
    - sensor.date
    - automation.update_power
    - switch.desk_lamp
